services:
  vm: # virtual machine
    container_name: seitelite-vm
    build:
      context: tools
      dockerfile: Dockerfile
    volumes:
      - ./:/SeiteLite
    ports:
      - 8080:80 # Apache
    extra_hosts:
      - 'host.docker.internal:host-gateway'
